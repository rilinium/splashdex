export const metadata = {
  title: 'Settings',
  description: 'Splashdex display and filter preferences.',
};

import SettingsPanel from '@/components/SettingsPanel';

export default function SettingsPage() {
  return <SettingsPanel />;
}
